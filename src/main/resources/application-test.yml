# 사용자 정의 속성
profile:
  value: 'test'

spring:
  data:
    mongodb:
      uri: mongodb://223.130.161.242:27017/chatdb
  datasource:
    url: ${TEST_DB_URL}
    username: ${TEST_DB_USERNAME}
    password: ${TEST_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver